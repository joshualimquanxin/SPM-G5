/**
 * Story 1.1 AC4: after login each role lands on the page most useful to it.
 * Every role lands on the home page until a feature page is built for it; add a case then
 * (e.g. coordinators -> review queue, story 4.1).
 */
export function homeFor(roleCode: string): string {
  switch (roleCode) {
    default:
      return '/'
  }
}
